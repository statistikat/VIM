% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vimpute_spec.R
\name{vimpute_spec}
\alias{vimpute_spec}
\alias{vs_ranger}
\alias{vs_xgboost}
\alias{vs_regularized}
\alias{vs_robust}
\alias{vs_gam}
\alias{vs_robgam}
\alias{print.vimpute_spec}
\title{Per-variable imputation specification for \code{vimpute()}}
\usage{
vimpute_spec(
  method,
  ...,
  formula = NULL,
  predictors = NULL,
  tune = FALSE,
  pmm = FALSE,
  pmm_k = NULL,
  pmm_k_method = NULL,
  makeNA = NULL,
  donorcond = NULL,
  uncert = NULL
)

vs_ranger(...)

vs_xgboost(...)

vs_regularized(...)

vs_robust(...)

vs_gam(...)

vs_robgam(...)

\method{print}{vimpute_spec}(x, ...)
}
\arguments{
\item{method}{Single string: a registered imputation method (see
\code{\link[=vimpute_methods]{vimpute_methods()}}).}

\item{...}{Named learner parameters for the method's learners (e.g.
\code{num.trees = 500} for \code{"ranger"}), validated eagerly.}

\item{formula}{One-sided (\code{~ x1 + s(x2)}) or two-sided formula for the
variable's model; only for methods with formula support. A one-sided
formula is completed with the variable as its left-hand side.}

\item{predictors}{Character vector of predictor columns (the
\code{predictors} equivalent, works for every method). A spec takes either
\code{formula} or \code{predictors}, not both.}

\item{tune}{Logical: tune this variable's learner (see
\code{\link[=vimpute_tune_control]{vimpute_tune_control()}}).}

\item{pmm}{Logical: predictive mean matching for this (numeric) variable.}

\item{pmm_k}{\code{NULL} or a positive integer: PMM donor count.}

\item{pmm_k_method}{\code{NULL}, \code{"mean"}, \code{"median"}, \code{"random"}, or a
function: PMM aggregation when \code{pmm_k > 1}.}

\item{makeNA}{\code{NULL} or a vector of values to be treated as missing for
this variable (as in \code{vimpute(makeNA = )}).}

\item{donorcond}{\code{NULL} or a donor condition string such as \code{">= 0"} (as
in \code{vimpute(donorcond = )}).}

\item{uncert}{Not a spec knob -- set \code{uncert} on the \code{\link[=vimpute]{vimpute()}} call;
supplying it here errors (it guards against silently treating it as a
learner parameter).}

\item{x}{A \code{vimpute_spec} object}
}
\value{
An object of class \code{vimpute_spec}.
}
\description{
Bundles everything \code{vimpute()} can configure for a single variable --
method, learner parameters, \code{formula} or \code{predictors}, \code{tune}, PMM
settings, \code{makeNA} values and a \code{donorcond} donor condition -- into one
object, instead of coordinating up to nine parallel per-variable
arguments. Pass a named list of specs as \code{vimpute(spec = )}; the reserved
name \code{".default"} supplies the spec for variables not listed (its method,
learner parameters and tune/PMM knobs; \code{formula}/\code{predictors}/\code{makeNA}/
\code{donorcond} are variable-specific and not allowed in \code{".default"}).
}
\details{
Learner parameters in \code{...} are validated \strong{eagerly} against the
method's learner parameter sets, so a typo fails at the constructor call,
not in iteration 7 of the imputation. \code{vs_ranger()}, \code{vs_xgboost()},
\code{vs_regularized()}, \code{vs_robust()}, \code{vs_gam()} and \code{vs_robgam()} are
shorthands for the built-in methods; methods added via
\code{\link[=register_vimpute_method]{register_vimpute_method()}} use \code{vimpute_spec("<name>", ...)}.

A variable with a spec uses that spec's settings exactly (no field-level
merging with \code{".default"}). \code{uncert} remains a call-level argument of
\code{\link[=vimpute]{vimpute()}} and is not a spec knob.
}
\examples{
\dontrun{
data(sleep)
res <- vimpute(sleep,
  spec = list(
    Sleep    = vs_ranger(num.trees = 300, tune = TRUE),
    NonD     = vs_robust(donorcond = ">= 0"),
    .default = vs_ranger()
  ),
  seed = 1)

# the same, as formula grammar:
res2 <- vimpute(sleep,
  Sleep ~ . | ranger(num.trees = 300, tune = TRUE),
  NonD  ~ . | robust(donorcond = ">= 0"),
  .default = vs_ranger(),
  seed = 1)
}
}
\seealso{
\code{\link[=vimpute]{vimpute()}}, \code{\link[=register_vimpute_method]{register_vimpute_method()}}
}
