% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vimpute_tune_control.R
\name{vimpute_search_space}
\alias{vimpute_search_space}
\title{The built-in tuning search space of a learner}
\usage{
vimpute_search_space(learner_id, task)
}
\arguments{
\item{learner_id}{Single string: an mlr3 learner id used by vimpute's
built-in methods, e.g. \code{"regr.ranger"}, \code{"classif.xgboost"},
\code{"regr.cv_glmnet"}, \code{"regr.lm_rob"}, \code{"regr.gam_imp"}.}

\item{task}{An mlr3 task (its number of rows scales the bounds and the
budget).}
}
\value{
A list with elements \code{space} (a \code{paradox::ParamSet}, or \code{NULL}
if the learner has no built-in space) and \code{n_evals} (the default
evaluation budget).
}
\description{
Returns the search space and default evaluation budget that
\code{vimpute(tune = TRUE)} uses for one of its built-in learners. The bounds
and budgets scale with the task size. Useful as the starting point for a
\code{search_space} hook in \code{\link[=register_vimpute_method]{register_vimpute_method()}} and for inspecting
what the tuner explores.
}
\examples{
\dontrun{
task <- mlr3::TaskRegr$new("t", backend = data.frame(y = 1:20, x = 1:20),
                           target = "y")
vimpute_search_space("regr.ranger", task)
}
}
\seealso{
\code{\link[=vimpute_tune_control]{vimpute_tune_control()}}, \code{\link[=register_vimpute_method]{register_vimpute_method()}}
}
