% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overimpute.R
\name{overimpute}
\alias{overimpute}
\alias{print.vimpute_overimpute}
\alias{plot.vimpute_overimpute}
\title{Overimputation: calibration diagnostic for an imputation model}
\usage{
overimpute(data, variable, ..., draws = 5, folds = 5, level = 0.9, seed = NULL)

\method{print}{vimpute_overimpute}(x, ...)

\method{plot}{vimpute_overimpute}(x, ...)
}
\arguments{
\item{data}{Dataset with (possibly) missing values.}

\item{variable}{Single column name: the numeric variable whose observed
cells are overimputed.}

\item{...}{Passed on to \code{\link[=vimpute]{vimpute()}} (e.g. \code{method}, \code{spec}, \code{uncert},
\code{sequential}). \code{m} is controlled via \code{draws} and cannot be supplied.}

\item{draws}{Number of multiple-imputation draws per cell (>= 2).}

\item{folds}{Number of folds the observed cells are split into (each fold
is set missing and overimputed in one \code{\link[=vimpute]{vimpute()}} run; capped at the
number of observed cells).}

\item{level}{Coverage level of the reported interval (default 0.9).}

\item{seed}{Optional single number: applied via \code{set.seed()} before
the fold split and the imputation runs.}

\item{x}{A \code{vimpute_overimpute} object}
}
\value{
A data.frame of class \code{vimpute_overimpute} with one row per
observed cell: \code{row} (row index in \code{data}), \code{observed},
\code{mean}, \code{lower}, \code{upper} (quantiles of the draws at
\code{level}), and \code{covered}. Attributes: \code{variable},
\code{level}, \code{draws}, \code{folds}. \code{print()} reports the
empirical coverage; \code{plot()} draws the observed-vs-imputed
calibration plot with intervals and the 45-degree line.
}
\description{
Answers "is my imputation model well calibrated?" without ground truth
(the analogue of \code{Amelia::overimpute()}, model-agnostic): the
\emph{observed} cells of one variable are treated as missing -- fold by
fold, so every observed cell is overimputed exactly once -- and imputed
with \code{draws} multiple imputations via \code{\link[=vimpute]{vimpute()}}. For each cell the
observed value is compared with the mean and a \code{level} interval of
its draws; a well-calibrated model covers roughly \code{level} of the
observed values. Any genuinely missing cells stay missing and are imputed
as part of the model, exactly as they would be in a real run.
}
\details{
The \code{...} arguments are passed on to \code{\link[=vimpute]{vimpute()}}, so the diagnostic runs
for any method, spec, or grammar configuration -- e.g.
\code{overimpute(dat, "y", method = "robust")} or
\code{overimpute(dat, "y", spec = list(y = vs_ranger(num.trees = 300)))}.
Note the cost: \code{folds x draws} imputation runs; lower \code{folds} and
\code{draws} (the defaults are modest) or use a fast method for large data.
For a stochastic spread of the draws keep an uncertainty source switched
on (the default \code{uncert = "pmm"}, or \code{boot}/\code{uncert}); with purely
deterministic settings all draws coincide and the intervals collapse.
}
\examples{
\dontrun{
data(sleep)
ov <- overimpute(sleep, "Sleep", method = "robust", sequential = FALSE,
                 draws = 5, folds = 5, seed = 1)
print(ov)   # empirical coverage of the 90\% intervals
plot(ov)
}
}
\seealso{
\code{\link[=vimpute]{vimpute()}}, \code{\link[=makeMissing]{makeMissing()}}, \code{\link[=evaluation]{evaluation()}}
}
\author{
Matthias Templ
}
