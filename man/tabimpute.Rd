% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabimpute.R
\name{tabimpute}
\alias{tabimpute}
\alias{tabimpute_available}
\title{Impute missing values using TabImpute}
\usage{
tabimpute_available()

tabimpute(
  data,
  variables = NULL,
  id_vars = NULL,
  mixed = TRUE,
  max_rows = NULL,
  max_cols = NULL,
  python = NULL,
  tabimpute_module = NULL,
  backend = c("auto", "imputepfn", "categorical", "v2"),
  device = "cpu",
  checkpoint_path = NULL,
  preprocessors = NULL,
  nhead = 2L,
  backend_max_rows = NULL,
  backend_max_chunks = NULL,
  seed = NULL,
  verbose = FALSE,
  imp_var = TRUE,
  imp_suffix = "imp",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame}, tibble, or \code{data.table}.}

\item{variables}{Character vector of variables to impute. If \code{NULL}, all
variables with missing values are considered, excluding \code{id_vars}.}

\item{id_vars}{Character vector of variables that must not be imputed or sent
to the backend.}

\item{mixed}{Logical. Whether to allow mixed numeric/categorical data to be
passed to the backend. If \code{FALSE}, non-numeric backend columns are rejected.}

\item{max_rows}{Optional maximum number of rows allowed.}

\item{max_cols}{Optional maximum number of columns allowed.}

\item{python}{Optional path to the Python executable used by \code{reticulate}.}

\item{tabimpute_module}{Optional Python module/object, or an R function for
testing, used instead of importing the default Python module.}

\item{backend}{Character string selecting the Python backend. \code{"auto"} uses
\code{TabImputeCategorical} when categorical columns are present and
\code{ImputePFN} otherwise. \code{"v2"} selects \code{TabImputeV2} and currently
supports numeric data only.}

\item{device}{Device string passed to the Python backend, for example
\code{"cpu"} or \code{"cuda"}.}

\item{checkpoint_path}{Optional path to a local TabImpute checkpoint. If
\code{NULL}, the Python package may try to download the checkpoint.}

\item{preprocessors}{Optional Python preprocessors passed to the backend
constructor.}

\item{nhead}{Number of attention heads passed to \code{ImputePFN} backends.}

\item{backend_max_rows}{Optional row chunk size passed to \code{ImputePFN}.}

\item{backend_max_chunks}{Optional maximum number of chunks passed to
\code{ImputePFN}.}

\item{seed}{Optional seed passed to the backend where supported.}

\item{verbose}{Logical. If \code{TRUE}, print informative messages.}

\item{imp_var}{Logical. If \code{TRUE}, add logical imputation indicator columns.}

\item{imp_suffix}{Suffix for imputation indicator columns.}

\item{...}{Additional arguments passed to the backend adapter.}
}
\value{
For \code{tabimpute_available()}, a logical scalar. For \code{tabimpute()}, an
imputed data set. \code{data.table} inputs return a \code{data.table}; other
inputs return a \code{data.frame}.
}
\description{
\code{tabimpute()} provides an experimental interface to a Python TabImpute
backend for zero-shot missing-data imputation in tabular data. The Python
dependency is optional and imported lazily via \code{reticulate} only when the
function is called on data with selected missing values.
}
\details{
TabImpute support is experimental. It provides single imputed values and is
not a replacement for multiple imputation. Benchmark results carefully before
using it in survey or statistical-production workflows. Observed values are
preserved, and only originally missing values in selected variables are
replaced.

The adapter targets the Python package \code{tabimpute} and its
\code{tabimpute.interface.ImputePFN},
\code{tabimpute.interface.TabImputeCategorical}, and
\code{tabimpute.tabimpute_v2.TabImputeV2} classes. The Python package downloads
model checkpoints from Hugging Face when no \code{checkpoint_path} is supplied,
so pass \code{checkpoint_path} for offline or production use.
}
\examples{
\dontrun{
data(sleep, package = "VIM")
if (tabimpute_available()) {
  tabimpute(sleep)
}
}
}
\seealso{
Other imputation methods:
\code{\link{hotdeck}()},
\code{\link{impPCA}()},
\code{\link{imputeRobust}()},
\code{\link{imputeRobustChain}()},
\code{\link{irmi}()},
\code{\link{kNN}()},
\code{\link{matchImpute}()},
\code{\link{medianSamp}()},
\code{\link{rangerImpute}()},
\code{\link{regressionImp}()},
\code{\link{sampleCat}()},
\code{\link{vimpute}()},
\code{\link{xgboostImpute}()}
}
\concept{imputation methods}
