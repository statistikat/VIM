% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vimmi.R
\name{with.vimmi}
\alias{with.vimmi}
\title{Evaluate an expression across all imputations}
\usage{
\method{with}{vimmi}(data, expr, ...)
}
\arguments{
\item{data}{A \code{vimmi} object}

\item{expr}{An expression to evaluate, e.g. \code{lm(y ~ x)}}

\item{...}{Currently unused}
}
\value{
A list of length \code{m} containing the result of evaluating
\code{expr} on each completed dataset
}
\description{
Applies an expression (typically a model fit) to each completed dataset
in a \code{vimmi} object. The results can be pooled using
\code{mice::pool()} or \code{mitools::MIcombine()}.
}
\examples{
\dontrun{
result <- vimpute(sleep, method = "ranger", m = 5, boot = TRUE, uncert = "normalerror")
fits <- with(result, lm(Sleep ~ Dream + Span))
# Pool with mice:
# mice::pool(fits)
}
}
