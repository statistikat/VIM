% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellgloc_utils.R
\name{.gloc_design_setup}
\alias{.gloc_design_setup}
\title{The design a fit with a missing categorical value as a level works on}
\usage{
.gloc_design_setup(data, design, cat_vars)
}
\arguments{
\item{data}{a data.frame containing the categorical variables.}

\item{design}{a one-sided formula, e.g. \code{~ .} or \code{~ 1}.}

\item{cat_vars}{character vector naming the categorical columns.}
}
\value{
\code{list(U, U_main, patterns)}; \code{U_main} is \code{U} for a
design without interaction terms, and \code{patterns} may be \code{NULL}.
}
\description{
The design (\code{.gloc_design}), its main-effects version and its level
combinations (\code{.gloc_design_aux}), with a combination table dropped when
its columns do not match. \code{imputeCellGLoc} uses it whenever no
categorical EM runs, and for the EM's second start, which is the robust start
of \code{categorical = "level"}; one function keeps the two identical.
}
\keyword{internal}
