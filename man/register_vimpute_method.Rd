% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vimpute_registry.R
\name{register_vimpute_method}
\alias{register_vimpute_method}
\title{Register an imputation method for \code{vimpute()}}
\usage{
register_vimpute_method(
  name,
  learner,
  packages = character(),
  setup = NULL,
  defaults = NULL,
  search_space = NULL,
  supports_formula = FALSE,
  fallback = "robust",
  validate = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{name}{Single character string: the method name to be used in
\code{vimpute(method = )}. Must not collide with a registered method unless
\code{overwrite = TRUE}. Built-in methods cannot be replaced or removed.}

\item{learner}{Named list with elements \code{regr} and/or \code{classif}, each a
character vector of mlr3 learner ids (candidates in preference order; the
first is the default, multiple candidates are compared by cross-validation
like the built-in \code{"regularized"} method). Methods registered with only a
\code{regr} (or only a \code{classif}) learner fall back to \code{fallback} for target
variables of the other type, with a warning.}

\item{packages}{Character vector of packages that must be installed when the
method is used (checked with \code{requireNamespace()} at \code{vimpute()} call
time, not at registration).}

\item{setup}{\code{NULL} or a function with no arguments, called once per
\code{vimpute()} call before the method's learners are constructed. Use it to
register custom mlr3 learners or load learner collections (e.g.
\code{function() library(mlr3extralearners)}).}

\item{defaults}{\code{NULL}, a named list of learner parameter values, or a
function \verb{function(task_type, nthread)} returning such a list
(\code{task_type} is \code{"regr"} or \code{"classif"}, \code{nthread} the thread count
vimpute chose for the data size). User-supplied \code{learner_params} override
these defaults.}

\item{search_space}{\code{NULL} or a function \verb{function(learner_id, task)}
returning \verb{list(space = paradox::ps(...), n_evals = <integer>)}, consulted
when \code{tune = TRUE}. Without it, tuning is skipped for the method with a
warning (as for unknown learners).}

\item{supports_formula}{Logical: can the method be used with the \code{formula}
argument of \code{vimpute()}? Formula-based imputation requires a learner that
models from a design matrix; the built-ins with formula support are
\code{"robust"}, \code{"regularized"}, \code{"gam"}, and \code{"robgam"}.}

\item{fallback}{Single method name used when \code{validate} rejects a variable
or a target type has no learner. Defaults to \code{"robust"}.}

\item{validate}{\code{NULL} or a function \verb{function(y_obs, data, variable)}
called during pre-checking for every variable the method is assigned to
(\code{y_obs}: the observed values of the target; \code{data}: the full dataset;
\code{variable}: the target's name). Return \code{NULL} to accept, a character
string (the warning message) to reject towards \code{fallback}, or
\code{list(reason = , fallback = )} to reject towards a specific method.
Fallbacks are validated in turn until a method accepts.}

\item{overwrite}{Logical: replace an existing registration of the same
name? Built-in methods can never be replaced.}
}
\value{
Invisibly, the registered method name.
}
\description{
\code{vimpute()} resolves its \code{method} argument through a package-level method
registry. The built-in methods (\code{"ranger"}, \code{"xgboost"}, \code{"regularized"},
\code{"robust"}, \code{"gam"}, \code{"robgam"}) are pre-registered; this function adds
(or, with \code{overwrite = TRUE}, replaces) a user-defined method backed by any
pair of mlr3 learners -- e.g. \code{regr.rpart}/\code{classif.rpart} from mlr3
itself, or learners from \code{mlr3extralearners} such as lightgbm -- without
modifying VIM. After registration the new name can be used anywhere the
built-in method names work: as a global \code{method}, in a per-variable method
list, and in method-keyed \code{learner_params}.
}
\details{
Uncertainty handling for registered methods: PMM (\code{uncert = "pmm"}, the
default, and \code{pmm = TRUE}) and \code{uncert = "midastouch"} work with any
method because they only use the method's predictions.
\code{uncert = "normalerror"}/\code{"resid"} and \code{boot = TRUE} derive residuals from
training predictions when the model object does not expose them.
}
\examples{
# a CART method backed by mlr3's rpart learners -- one call, no VIM patching
register_vimpute_method("cart",
  learner  = list(regr = "regr.rpart", classif = "classif.rpart"),
  packages = "rpart")
"cart" \%in\% vimpute_methods()
\donttest{
data(sleep)
res <- vimpute(sleep[, c("Sleep", "Dream", "Span")], method = "cart",
               sequential = FALSE)
}
unregister_vimpute_method("cart")
}
\seealso{
\code{\link[=vimpute_methods]{vimpute_methods()}}, \code{\link[=unregister_vimpute_method]{unregister_vimpute_method()}}, \code{\link[=vimpute]{vimpute()}}

Other vimpute method registry: 
\code{\link{unregister_vimpute_method}()},
\code{\link{vimpute_methods}()}
}
\concept{vimpute method registry}
