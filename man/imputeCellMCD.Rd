% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputeCellEM.R
\name{imputeCellMCD}
\alias{imputeCellMCD}
\title{Cellwise MCD-based imputation for mixed data}
\usage{
imputeCellMCD(
  data,
  maxit = 50,
  eps = 0.005,
  method = "tukey",
  alpha = NULL,
  mcd_alpha = 0.75,
  hard_threshold = 0.5,
  mcd_observed = "all",
  init_method = "median",
  uncert = "conditional",
  m = 1L,
  boot = FALSE,
  trace = FALSE
)
}
\arguments{
\item{data}{a \code{data.frame} with missing values (mixed continuous
and categorical variables are supported).}

\item{maxit}{maximum number of iterations (default: 50).}

\item{eps}{convergence tolerance (default: 5e-3).}

\item{method}{weight function for cell weights: \code{"tukey"} (default)
or \code{"huber"}.}

\item{alpha}{tuning constant.  \code{NULL} (default) uses 4.685 for
Tukey and 1.345 for Huber.}

\item{mcd_alpha}{MCD concentration parameter (default: 0.75).}

\item{hard_threshold}{numeric in \eqn{[0, 1]}.  Before iteration,
observed cells with initial MCD weight below this threshold are set
to missing and re-imputed (detect-once preprocessing).  Set to
\code{NULL} or \code{0} to disable (default: 0.5).}

\item{mcd_observed}{strategy for covariance estimation:
\code{"all"} (default) runs MCD on all data including imputed values;
\code{"weighted"} uses \code{cellWise::cwLocScat} with imputed cells
receiving weight 0 (requires the \pkg{cellWise} package);
\code{"pairwise"} uses pairwise robust (Gnanadesikan--Kettenring)
covariances on observed cells only.}

\item{init_method}{initialisation for missing values before iteration:
\code{"median"} (default), \code{"knn"}, or \code{"irmi"}.}

\item{uncert}{imputation uncertainty: \code{"conditional"} (default)
adds noise from the conditional normal distribution.}

\item{m}{number of multiple imputations (default: 1). If \code{m > 1},
a list of imputed datasets is returned.}

\item{boot}{logical; intended to add bootstrap parameter uncertainty across
the \code{m} imputations. \strong{Currently ineffective}: the imputation
step is a per-variable robust regression (\code{cellIRWLS}), so resampling
the location/covariance estimate leaves the imputations essentially
unchanged (measured between-imputation variance is not inflated relative to
\code{boot = FALSE}). Proper parameter-uncertainty propagation requires
bootstrapping the per-variable regression coefficients and is not yet
implemented; see the note below. Default \code{FALSE}.}

\item{trace}{logical; if \code{TRUE}, print progress information.}
}
\value{
A list with components:
\item{data_imputed}{the imputed \code{data.frame}.}
\item{cellweights}{\eqn{n \times p} matrix of cell weights.
Continuous observed cells have values in \eqn{[0, 1]};
categorical columns always have weight 1 (cellwise detection
is only applied to continuous variables).}
\item{mu}{robust location estimate (continuous variables).}
\item{Sigma}{robust covariance estimate (continuous variables).}
\item{converged}{logical indicating convergence.}
\item{iterations}{number of iterations performed.}
}
\description{
Extends the cellwise MCD approach (Raymaekers & Rousseeuw 2024) to
mixed continuous + categorical data.  Uses MCD for robust covariance
estimation of the continuous block, computes cellwise weights from
conditional residuals, then imputes missing values via conditional
expectations (continuous) and weighted multinomial regression
(categorical).  Iterates until convergence.
}
\note{
Multiple imputation with proper parameter-uncertainty propagation is
not yet implemented for this method (see \code{boot}). \code{m > 1} returns
repeated imputations whose between-imputation variability comes only from
the stochastic uncertainty step (\code{uncert}) and estimator randomness,
so downstream Rubin pooling understates total variance. Treat the current
\code{m > 1} output as approximate.
}
\examples{
\dontrun{
data(sleep, package = "VIM")
result <- imputeCellMCD(sleep)
head(result$data_imputed)

# Inspect cell weights
image(result$cellweights, main = "Cell weights")

# With pairwise robust covariance
result2 <- imputeCellMCD(sleep, mcd_observed = "pairwise", trace = TRUE)
}

}
\references{
Raymaekers, C. and Rousseeuw, P.J. (2024).  The cellwise minimum
covariance determinant estimator.  \emph{Journal of the American
Statistical Association}, \strong{119}(545), 576--588.
}
\seealso{
\code{\link{imputeCellIRMI}}, \code{\link{imputeCellM}},
\code{\link{imputeCellEM}}

Other imputation methods: 
\code{\link{hotdeck}()},
\code{\link{impPCA}()},
\code{\link{imputeCellEM}()},
\code{\link{imputeCellIRMI}()},
\code{\link{imputeCellM}()},
\code{\link{imputeCellwise}()},
\code{\link{imputeRobust}()},
\code{\link{imputeRobustChain}()},
\code{\link{irmi}()},
\code{\link{kNN}()},
\code{\link{matchImpute}()},
\code{\link{medianSamp}()},
\code{\link{rangerImpute}()},
\code{\link{regressionImp}()},
\code{\link{sampleCat}()},
\code{\link{vimmi}},
\code{\link{vimpute}()},
\code{\link{xgboostImpute}()}
}
\author{
Matthias Templ
}
\concept{imputation methods}
