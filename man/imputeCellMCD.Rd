% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputeCellEM.R
\name{imputeCellMCD}
\alias{imputeCellMCD}
\title{Cellwise MCD-based imputation for mixed data}
\usage{
imputeCellMCD(
  data,
  maxit = 50,
  eps = 0.005,
  method = "tukey",
  alpha = NULL,
  mcd_alpha = 0.75,
  hard_threshold = 0.5,
  mcd_observed = "all",
  uncert = "conditional",
  trace = FALSE
)
}
\arguments{
\item{data}{data.frame with missing values (mixed continuous + categorical)}

\item{maxit}{maximum iterations (default: 50)}

\item{eps}{convergence tolerance (default: 5e-3)}

\item{method}{weight function for cell weights: \code{"tukey"} (default)
or \code{"huber"}}

\item{alpha}{tuning constant (default: NULL, uses 4.685 for tukey)}

\item{mcd_alpha}{MCD concentration parameter (default: 0.75)}

\item{hard_threshold}{numeric in \eqn{[0, 1]}.  After convergence,
cells with weight below this value are flagged as contaminated
(default: 0.5).}

\item{mcd_observed}{strategy for MCD estimation: \code{"all"} (default),
\code{"weighted"}, or \code{"pairwise"}.}

\item{uncert}{imputation uncertainty: \code{"conditional"} (default) or
\code{"pmm"}}

\item{trace}{logical; print progress}
}
\value{
A list with components:
\item{data_imputed}{the imputed data.frame}
\item{cellweights}{n x p matrix of cell weights}
\item{mu}{robust location estimate (continuous variables)}
\item{Sigma}{robust covariance estimate (continuous variables)}
\item{converged}{logical}
\item{iterations}{number of iterations}
}
\description{
Extends the cellwise MCD approach (Raymaekers & Rousseeuw 2024) to
mixed continuous + categorical data.  Uses MCD for robust covariance
estimation of the continuous block, computes cellwise weights from
conditional residuals, then imputes missing values via conditional
expectations (continuous) and weighted multinomial regression
(categorical).  Iterates until convergence.
}
\author{
Matthias Templ
}
