% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vimpute_tune_control.R
\name{vimpute_tune_control}
\alias{vimpute_tune_control}
\title{Control the hyperparameter tuning of \code{vimpute()}}
\usage{
vimpute_tune_control(
  budget = NULL,
  folds = NULL,
  tuner = "random_search",
  batch_size = 1L
)
}
\arguments{
\item{budget}{\code{NULL} or a single positive integer: the number of
configurations the tuner evaluates. \code{NULL} (default) keeps the
per-learner, data-size-dependent budgets of the built-in search spaces
(see \code{\link[=vimpute_search_space]{vimpute_search_space()}}).}

\item{folds}{\code{NULL} or a single integer >= 2: the cross-validation folds
used during tuning (capped by what the data supports). \code{NULL} (default)
keeps the heuristic of 5 folds up to 3000 rows, 3 above.}

\item{tuner}{Single string: an mlr3tuning tuner id, e.g.
\code{"random_search"} (default) or \code{"grid_search"}. Tuners beyond the
defaults may require additional packages.}

\item{batch_size}{Single positive integer: configurations evaluated per
tuner batch. The default \code{1} keeps the random-search RNG consumption
machine-independent, so \verb{seed =} reproduces tuning results everywhere;
larger values evaluate in batches (faster with a parallel
\code{future::plan}, still reproducible for a fixed value).}
}
\value{
An object of class \code{vimpute_tune_control}.
}
\description{
Bundles the tuning controls that \code{vimpute(tune = TRUE)} previously
hard-coded: the evaluation budget, the cross-validation folds of the
tuning resampling, the mlr3tuning tuner and its batch size. Pass the
result to \code{vimpute(tune_control = )}. With \code{m > 1} the control applies to
the single tuning run (run 1), whose parameters all \code{m} imputations share.
}
\details{
Nested resampling is intentionally not offered: the goal of tuning inside
an imputation loop is good imputations, not an unbiased estimate of the
learner's generalisation error, and the tuned-vs-default comparison that
\code{vimpute()} runs on a fresh resampling already guards against tuning that
overfits the folds.
}
\examples{
\dontrun{
data(sleep)
res <- vimpute(sleep[, c("Sleep", "Dream", "Span")], method = "ranger",
               tune = TRUE, sequential = FALSE,
               tune_control = vimpute_tune_control(budget = 10, folds = 3))
attr(res, "tuning_log")
}
}
\seealso{
\code{\link[=vimpute]{vimpute()}}, \code{\link[=vimpute_search_space]{vimpute_search_space()}}
}
