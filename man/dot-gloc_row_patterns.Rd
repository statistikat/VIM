% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellgloc_utils.R
\name{.gloc_row_patterns}
\alias{.gloc_row_patterns}
\title{Level combinations taken from the distinct rows of a design}
\usage{
.gloc_row_patterns(U, U_main = NULL)
}
\arguments{
\item{U}{\eqn{n x q} design matrix.}

\item{U_main}{the main-effects design, or \code{NULL}.}
}
\value{
a \code{patterns} list as described in \code{.gloc_design_aux}.
}
\description{
The fallback when a caller passes no \code{patterns} (direct calls of the
internal functions): every distinct row of \code{U} counts as a level
combination. A probability-weighted row would then count as a combination
of its own, so a caller with such rows must pass \code{patterns} with an
\code{NA} id for them. The row keys are built once per call, not once per
column.
}
\keyword{internal}
