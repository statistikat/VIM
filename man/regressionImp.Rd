% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressionImp.R
\name{regressionImp}
\alias{regressionImp}
\title{Regression Imputation (via vimpute)}
\usage{
regressionImp(
  formula,
  data,
  family = "AUTO",
  robust = FALSE,
  imp_var = TRUE,
  imp_suffix = "imp",
  mod_cat = FALSE
)
}
\arguments{
\item{formula}{model formula to impute one variable}

\item{data}{A data.frame containing the data}

\item{family}{ignored in this wrapper version (only "AUTO"-like behaviour via vimpute)}

\item{robust}{logical; if TRUE use method = "robust", otherwise "regularized"}

\item{imp_var}{logical; if TRUE create TRUE/FALSE variables indicating imputation status}

\item{imp_suffix}{suffix used for TF imputation variables}

\item{mod_cat}{ignored in this wrapper version (classification handled by vimpute)}
}
\value{
the imputed data set (same class as \code{data})
}
\description{
Impute missing values based on a regression / classification model.
}
\details{
By default (\code{\link[=lm]{lm()}} for numeric responses and \code{\link[=glm]{glm()}} for binary responses,
with any number of predictors) the imputation is deterministic and matches a
plain regression fit. When the design is rank-deficient (collinear predictors
or \eqn{p \ge n}) or the response is a multi-level factor, it falls back to
the regularized (glmnet) path via \code{\link[=vimpute]{vimpute()}} with \code{method = "regularized"}.
If \code{robust = TRUE}, \code{\link[=vimpute]{vimpute()}} with \code{method = "robust"}
(\link[robustbase:lmrob]{lmrob()} / \link[robustbase:glmrob]{glmrob()}) is used.
}
\seealso{
Other imputation methods: 
\code{\link{hotdeck}()},
\code{\link{impPCA}()},
\code{\link{imputeCellEM}()},
\code{\link{imputeCellIRMI}()},
\code{\link{imputeCellM}()},
\code{\link{imputeCellMCD}()},
\code{\link{imputeCellwise}()},
\code{\link{imputeRobust}()},
\code{\link{imputeRobustChain}()},
\code{\link{irmi}()},
\code{\link{kNN}()},
\code{\link{matchImpute}()},
\code{\link{medianSamp}()},
\code{\link{rangerImpute}()},
\code{\link{sampleCat}()},
\code{\link{vimmi}},
\code{\link{vimpute}()},
\code{\link{xgboostImpute}()}
}
\concept{imputation methods}
