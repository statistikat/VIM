% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cellgloc_categorical.R
\name{.gloc_na_level_start}
\alias{.gloc_na_level_start}
\title{The categorical EM's second start}
\usage{
.gloc_na_level_start(X, M, data, design, cat_vars, cand)
}
\arguments{
\item{X, M}{the continuous data and their missing mask.}

\item{data, design, cat_vars}{as in \code{imputeCellGLoc}.}

\item{cand}{the EM's pseudo-row table, from \code{.gloc_cat_candidates}.}
}
\value{
\code{list(W, B)}.
}
\description{
The robust start of \code{categorical = "level"}: a missing categorical value
is a level of its own and every row is fitted (\code{.gloc_design_setup},
\code{.gloc_start_robust} without \code{fit_rows}). Its flags are kept, and its
coefficients are carried into the EM's design by \code{.gloc_level_B}. The
level posteriors are left to the caller, which starts them from the priors
alone, as for the first start.
}
\keyword{internal}
